name: SEI2Hub CI

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

jobs:
  build-and-lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Run dummy test (Placeholder)
      run: |
        echo "CI pipeline initialized successfully!"
        echo "Please add language-specific linters and tests here later."